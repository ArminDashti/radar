# Add Token Expiry Awareness

Decode the JWT expiry client-side or expose a session endpoint so the UI can redirect before an authenticated request fails. Medium effort; improves long-lived dashboard sessions.
