# Publish radar-webui grid fill + ICMP display

- **When:** 2026-08-25 (width-fill fix redeploy ~16:15)
- **Stack:** radar-webui
- **Image:** radar-webui:latest
- **Domain:** radar.xaigrok.ir
- **Network:** t3-net
- **Deploy:** `.armin/docker-scripts/run-on-docker-server.ps1` (build local → save → scp → load → compose up)

## Verify

- Container `radar-webui` Up on `t3-net`
- HTTPS `radar.xaigrok.ir` → 200
- Cert issuer: Let's Encrypt; subject CN=radar.xaigrok.ir
- SoftEther SNI `ovpbackup.xaigrok.ir` left untouched

## App changes shipped

- Latency squares size from **column width only** (many rows scroll vertically; no longer `min(width, height)` which left half the page empty)
- Sub-1 ms latency shown as `<1 ms` in tooltips and Test summary

## Result

Success
