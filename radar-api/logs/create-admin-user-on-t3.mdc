# Create admin user on Irancell-T3 (radar-api)

Date and time: 2026-08-26 15:20:00

## Result
- Created Radar DB user `admin` with role `admin` in container `radar-pgsql` (database `radar`)
- User id: `8`
- Login verified via `POST /api/auth/login` (container network and `https://radar.xaigrok.ir`) → `username=admin`, `role=admin`, JWT issued

## Commands
- Listed users: only seed `armin` (admin) existed
- Generated bcrypt hash (cost 10)
- `INSERT INTO users (username, password_hash, role) … ON CONFLICT …`
- Fixed first insert (hash had literal quote characters); updated to clean 60-char bcrypt hash
- Verified login returns JWT with role `admin`

## Warnings
- Password not written in this log
- Demo-style shared password; rotate for production if needed
