# Publish radar-api + radar-agent on Irancell-T3

Date and time: 2026-08-24 11:06:15

## Result
- Hostname: `https://radar.xaigrok.ir` → `radar-api:8088` on `t3-net`
- Stacks: `radar-api` (API + Postgres), `radar-agent` (private probe, hub `http://radar-api:8088`)
- Image tags: `radar-api:latest`, `radar-agent:latest`
- Let's Encrypt: issued for `radar.xaigrok.ir` (expires 2026-11-22); PEM installed; renew hook updated
- SoftEther: `ovpbackup.xaigrok.ir` passthrough left intact (HUP reload only)

## Commands / notes
- Docker packaging under each repo (`dockerfile`, `docker-compose.yml`, `.armin/docker-scripts`)
- Deploy via `run-on-docker-server.ps1` (`ssh t3`, volumes under `/cloud-admin/docker-volumes/`)
- HAProxy: SNI + Host ACL + `be_radar_api`
- Certbot standalone HTTP-01 with existing SSH port-80 pre/post hooks

## Warnings
- Agent is not publicly exposed (no hostname)
- Demo credentials remain for bootstrap; rotate for production

## Follow-up 2026-08-24 11:28:28
- HAProxy restarted to remount config; `https://radar.xaigrok.ir/` now serves radar-webui (200 HTML); `/api/auth/login` 200; SoftEther SNI OK.

## Follow-up 2026-08-24 12:45:00
- Redeployed `radar-api:latest` (logo upload/serve, `radar-logos-vol` → `/data/logos`) and `radar-webui:latest` (PWA + full-width latency grid + admin logo upload)
- Verify: LE issuer `Let's Encrypt` for `radar.xaigrok.ir`; root/manifest/sw 200; login + grid/hosts 200; SoftEther SNI still answers for `ovpbackup.xaigrok.ir`
- Image tags: `radar-api:latest`, `radar-webui:latest`

## Follow-up 2026-08-24 17:35:00
- Redeployed `radar-api:latest` + `radar-webui:latest` (public grid/probes GETs, host URL in grid, ICMP+HTTP defaults, fixed 11px squares, red × timeouts, dark neutral theme, public `/hosts`, admin in-place edit + Save)
- Verify: LE issuer `Let's Encrypt` (`YE1`) for `radar.xaigrok.ir`; SoftEther SNI for `ovpbackup.xaigrok.ir` OK; anonymous `/hosts` 200; `/api/grid/hosts` + `/api/probes` 200; `/api/hosts` still 401; all 48 active endpoints HTTP+ICMP enabled
- Image tags: `radar-api:latest`, `radar-webui:latest`

## Follow-up 2026-08-25 17:05:00
- Redeployed `radar-api:latest` + `radar-agent:latest` (ICMP via OS `ping` only; removed raw sockets)
- Agent connected to `http://radar-api:8088`; submitted 96 samples; `/bin/ping` present in both containers
- Verify: LE issuer `Let's Encrypt` (`YE1`) for `radar.xaigrok.ir` (exp 2026-11-22); root 200; `/api/probes` 200; SoftEther SNI for `ovpbackup.xaigrok.ir` OK
- Note: sub-1 ms ICMP to some Iranian edges (e.g. `bale.ai`) matches live OS `ping` on T3

## Follow-up 2026-08-26 10:51:00
- Migrated compose project name from `radar-api` / `radar-agent` → shared `radar` (volumes reused with warning)
- Redeployed `radar-api:latest` + `radar-agent:latest` via `run-on-docker-server.ps1` (`ssh t3`, `t3-net`, empty `publish_port`)
- Agent connected to `http://radar-api:8088`; `/bin/ping` OK in api + agent
- Verify: LE issuer `Let's Encrypt` (`YE1`) for `radar.xaigrok.ir` (exp 2026-11-22); root 200; `/api/probes` 200; SoftEther SNI for `ovpbackup.xaigrok.ir` OK
- Left `radar-webui` running (unchanged)

## Follow-up 2026-08-26 16:05:00
- Redeployed stack `radar` on `radar.xaigrok.ir`: `radar-api:latest`, `radar-agent:latest`, `radar-webui:latest`
- Scripts: `update-on-docker-server.ps1` (api + agent); created webui `.armin/docker-scripts/update-on-docker-server.*`; removed stale `/radar-webui` name conflict then `compose up`
- HAProxy: existing SNI + `be_radar_webui` / `be_radar_api`; HUP reload only
- Let's Encrypt: existing PEM issuer `YE1` (exp 2026-11-22); renew hook already lists `radar.xaigrok.ir`
- Verify: root 200; `/api/probes` 200; `/api/hosts` 401 (auth); agent submitted samples; SoftEther SNI for `ovpbackup.xaigrok.ir` OK

