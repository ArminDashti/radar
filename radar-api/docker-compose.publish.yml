# Optional overlay: host port publish (local / explicit publish_port).
# Scripts add -f docker-compose.publish.yml only when publish_port is non-empty.

services:
  radar-api:
    ports:
      - target: ${INTERNAL_PORT:-8088}
        published: ${PUBLISH_PORT:-8088}
        protocol: tcp
